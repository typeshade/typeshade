---
active: true
derived: false
level: 8.24
links: []
normative: true
ref: ''
references:
- keyword: Rule 8.24
  path: scripts/user-journey.ts
  type: file
- keyword: Rule 8.24
  path: src/compiler/ts/host-entry.test.ts
  type: file
- keyword: Rule 8.24
  path: src/compiler/ts/host-face.ts
  type: file
- keyword: Rule 8.24
  path: src/core/host-entry.ts
  type: file
reviewed: ahpFHkv7GI2Xo750nJtiSuVRusWLKYWEBXdTE2o16PY=
verification: test
---

**Rule 8.24.** An exported `@compute` entry is called from host code as `entry(bindings, workgroups)`.
`bindings` has one property for each binding the entry reaches through its calls, and no other, each the binding's host value (Rule 8.21); `workgroups` is the workgroup count, `n` or `[x, y?, z?]`, dispatched as written with no bound added; the call returns a promise, and when it resolves every storage binding the entry wrote has been read back into the caller's value in place.
It runs on WebGPU where there is a device, and otherwise on the CPU tier (Rule 11.7), every invocation of every workgroup in turn, `z`, then `y`, then `x`, with the workgroup memory zeroed for each workgroup; an entry that reaches a barrier needs WebGPU, and where there is none the call is refused with a `TypeError` naming the barrier and its line.
An entry that reaches a binding the call cannot pack (a texture, a sampler, an emulated `f64`), and a vertex or fragment entry, stays `never` in the host view, naming the work that adds it.

- Rationale: an entry is the escape hatch below the loop that becomes a kernel (change 0013), and it runs as its author wrote it: WGSL's unit, the author's own bound check, no guard added.
  The call replaces what a host otherwise writes by hand around `compile()` and `reflect()`: the device, the module, the layouts, the packing, the dispatch and the readback.
  The workgroup memory and the order of invocations on the CPU tier are those of the interpreter's own dispatch, so the two agree; a barrier needs the interpreter's generators to run in lockstep, which the runtime does not ship.
- Derives from: change `0016` in `changes/` (roadmap item 16, second half); Rule 8.21; `docs/dx.md` principle 6.
- Enforced by: `callCompute` in `src/core/host-entry.ts`, what the generated module calls, and `computeFace` in `src/compiler/ts/host-face.ts`, which writes the entry's layouts and view type; pinned by `src/compiler/ts/host-entry.test.ts`, which holds the CPU tier equal to the interpreter's dispatch and checks each refusal's text, and by the import journey in `scripts/user-journey.ts`, which calls two entries on WebGPU in Chromium from the packed tarball.